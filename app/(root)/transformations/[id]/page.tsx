import React from 'react'

const TransformationsPage = () => {
  return (
    <div>TransformationsPage</div>
  )
}

export default TransformationsPage


//each transformation will be different 
//so it won't will happen on local host 300 like
//localhost:3000/transformation but it will not happen like this
// each transformation will have its own id  look like this
//localhost:3000/transformation/[id] -we will use dynamic routes

//this route will be for get requests
